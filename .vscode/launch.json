{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run App",
            "preLaunchTask": "tsc: build - tsconfig.json",
            "program": "${workspaceFolder}/src/App.ts",
            "outFiles": [
                "${workspaceFolder}/build/**/*.js"
            ],
        },
    ]
}